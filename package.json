{
  "name": "tradeflow-pro",
  "version": "1.0.0",
  "description": "TradeFlow Pro - Binary Options Trading Platform",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "devDependencies": {}
}
